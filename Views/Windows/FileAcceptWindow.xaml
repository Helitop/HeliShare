<ui:FluentWindow     x:Class="HeliShare.Views.Windows.FileAcceptWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:HeliShare.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    MinHeight="200"
    MinWidth="450"
    MaxHeight="200"
    MaxWidth="450"
    d:DataContext="{d:DesignInstance local:MainWindow,
                                     IsDesignTimeCreatable=True}"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    ExtendsContentIntoTitleBar="True"
    mc:Ignorable="d" ResizeMode="NoResize" Topmost="True">
    <Grid Margin="20">
        <StackPanel>
            <ui:TextBlock Text="Подтвердите получение файла" FontSize="25" Margin="0,0,0,10" HorizontalAlignment="Center"/>
            <Image Source="{Binding FileIcon}" 
               Width="32" 
               Height="32" 
               Margin="0,0,10,0"
               RenderOptions.BitmapScalingMode="HighQuality"/>
            <ui:TextBlock Text="{Binding FileName}" HorizontalAlignment="Center" TextWrapping="WrapWithOverflow"/>
        </StackPanel>
        <Grid VerticalAlignment="Bottom" HorizontalAlignment="Stretch">
            <ui:Button Content="Принять" Appearance="Primary" Click="Yes" HorizontalAlignment="Left"/>
            <ui:Button Content="Отказ" Click="No" HorizontalAlignment="Right"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
